# macOS
.DS_Store

# Editor / OS scraps
*.swp
Thumbs.db

# Firebase CLI local output (never commit)
.firebase/
firebase-debug.log
firestore-debug.log

# local demo copy made by tools/refresh-demo.sh — never commit it
_demo/
